---
import { Calendar, ArrowRight, BookOpen } from 'lucide-react';
import { getPublishedArtikelList } from '../../lib/api';

const articles = await getPublishedArtikelList();
const latestArticles = articles.slice(0, 3);

const formatDate = (dateString: string) => {
  const options: Intl.DateTimeFormatOptions = { day: 'numeric', month: 'long', year: 'numeric' };
  return new Date(dateString).toLocaleDateString('id-ID', options);
};
---

<section id="artikel" class="py-20 bg-[#FAF9F6] scroll-mt-20">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    
    <!-- Header -->
    <div class="flex flex-col md:flex-row md:items-end md:justify-between mb-12">
      <div>
        <div class="inline-flex items-center text-sm font-bold text-gray-500 uppercase tracking-wider mb-2">
          <BookOpen className="w-4 h-4 mr-2 text-gray-400" />
          ARTIKEL & BERITA PROPERTI
        </div>
        <h2 class="text-3xl sm:text-4xl font-bold tracking-tight text-[#17201C] font-serif">
          Kabar & Tips Terbaru
        </h2>
      </div>

      <div class="mt-4 md:mt-0">
        <a 
          href="/artikel" 
          class="inline-flex items-center text-base font-bold text-[#0E3B2E] hover:text-[#07241C] group"
        >
          <span>Lihat Semua Artikel</span>
          <ArrowRight className="w-4 h-4 ml-1.5 transition-transform group-hover:translate-x-1" />
        </a>
      </div>
    </div>

    <!-- 3 Cards Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {latestArticles.map((artikel) => (
        <article class="bg-white rounded-2xl shadow-sm hover:shadow-xl transition-all duration-300 overflow-hidden border border-gray-100 flex flex-col h-full group">
          <a href={`/artikel/${artikel.slug}`} class="block relative h-56 overflow-hidden">
            <img 
              src={artikel.gambar_utama_url} 
              alt={artikel.judul} 
              class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
            />
          </a>
          
          <div class="p-6 sm:p-7 flex-grow flex flex-col">
            <div class="flex items-center text-sm font-semibold text-[#4A5568] mb-3">
              <Calendar className="w-4 h-4 mr-2 text-[#0E3B2E]" />
              {formatDate(artikel.tanggal_publish)}
            </div>
            
            <a href={`/artikel/${artikel.slug}`} class="block mb-3">
              <h3 class="text-xl font-bold text-[#17201C] font-serif group-hover:text-[#0E3B2E] transition-colors leading-snug">
                {artikel.judul}
              </h3>
            </a>
            
            <p class="text-sm sm:text-base text-[#4A5568] mb-6 line-clamp-3 flex-grow leading-relaxed">
              {artikel.excerpt}
            </p>
            
            <div class="mt-auto pt-4 border-t border-gray-100">
              <a href={`/artikel/${artikel.slug}`} class="text-[#0E3B2E] hover:text-[#07241C] font-bold text-sm sm:text-base flex items-center transition-colors group/link">
                <span>Baca Selengkapnya</span>
                <ArrowRight className="ml-1.5 w-4 h-4 transition-transform group-hover/link:translate-x-1" />
              </a>
            </div>
          </div>
        </article>
      ))}
    </div>
    
  </div>
</section>

